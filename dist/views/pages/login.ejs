<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head'); %>    
    <title>Banco</title>
</head>
<body>
    <header>
        <%- include("../partials/headerLogin"); %>
    </header>
    <main>
        <div class="container-fluid mb-5 mt-5 ">
            <div class="row">
                <div class="col d-flex justify-content-center">
                    <h1 class="mt-5 mb-5 text-login">Login</h1>
                </div>
                <% if(msg != ''){%>
                    <div class="col-12  d-flex justify-content-center alert alert-warning">
                        Usuário ou senha inválida
                    </div>
                <% } %>
            </div>
            <div class="row boxlogin">
                <div class="col">
                    <form class="form" action="/login" method="POST">
                        <div class="form-group">
                            <label for="email">Email</label>
                            <input class="form-control" type="email" id="email" name="email" placeholder="meuemail@email.exemplo" required>
                        </div>
                        <div class="form-group">
                            <label for="senha">Senha</label>
                            <input class="form-control" type="password"  id="senha" name="senha" required>
                        </div>                      
                        <input class="btn btn-warning btn-login" type="submit" value="Entrar">
                    </form>
                </div>
            </div>
        </div>
    </main>
    <footer>
        <%- include('../partials/footer'); %>
    </footer>
    <%- include('../partials/scripts'); %>
</body>
</html>